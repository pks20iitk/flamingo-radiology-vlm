# Core ML / Flamingo inference
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.42.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Vision
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Fine-tuning (LoRA / QLoRA)
peft>=0.10.0
trl>=0.8.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0.0

# HTTP
requests>=2.31.0

# Logging & CLI
loguru>=0.7.0
rich>=13.0.0

# Retry
tenacity>=8.2.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: flash attention (highly recommended for A100/H100)
# pip install flash-attn --no-build-isolation

# Optional: experiment tracking
# wandb>=0.17.0
